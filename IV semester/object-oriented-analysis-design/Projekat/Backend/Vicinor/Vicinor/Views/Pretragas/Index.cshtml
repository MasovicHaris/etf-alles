@model IEnumerable<Vicinor.Model.Pretraga>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TimeOfSearch)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TimeOfSearch)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PretragaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.PretragaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PretragaId })
        </td>
    </tr>
}

</table>
