@model IEnumerable<Vicinor.Model.Lokacija>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.X)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Y)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.X)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Y)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LokacijaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LokacijaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LokacijaId })
        </td>
    </tr>
}

</table>
